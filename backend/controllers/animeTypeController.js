const {Type} = require('../models/models');
const apiError = require('../error/ApiError');

class AnimeTypeController {
    async create(req, res) {

    }

    async getAll(req, res) {

    }
}

module.exports = new AnimeTypeController()